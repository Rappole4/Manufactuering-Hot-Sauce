<% @sauces.each do |sauce| %>
    <div class="product">
      <%=link_to new_order_path(@order) do %>
        <div class="p-image sauce_<%=sauce.id%>" style='background-image: url("<%=sauce.image%>")'></div>
      <%end%>
      <div class="p-name">
        <%= sauce.name %>
      </div>

    </div>
<% end %>
